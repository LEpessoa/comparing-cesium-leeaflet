import { Component, Input, input } from '@angular/core';

@Component({
  selector: 'app-styler-interface',
  standalone: true,
  imports: [],
  templateUrl: './styler-interface.component.html',
  styleUrl: './styler-interface.component.scss'
})
export class StylerInterfaceComponent {

  // @Input() styleGenerator:StyleGenerator;
  // @Input() styleLoader:StyleLoader;
  // @Input() mapProvider:MapProvider;
  // @Input() attributesFetcher:AttributesFetcher;
  // @Input() styleManager:StyleManager;

}

